<% content_for :title, "New Restaurant - MenuMate QR" %>

<div class="restaurants-form">
  <div class="mb-8">
    <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
      <div>
        <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 mb-2">New Restaurant</h1>
        <p class="text-gray-600 text-lg">Add a new restaurant to your collection</p>
        <p class="text-sm text-indigo-600 mt-2">
          <%= link_to "Or use our guided setup wizard →", onboarding_index_path, class: "font-semibold hover:underline" %>
        </p>
      </div>
      <% begin %>
        <%= link_to restaurants_path, class: "bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold py-3 px-6 rounded-xl shadow-md hover:shadow-lg transition-all duration-200 flex items-center gap-2" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          <span>Back to Restaurants</span>
        <% end %>
      <% rescue NoMethodError, NameError %>
        <%= link_to "/restaurants", class: "bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold py-3 px-6 rounded-xl shadow-md hover:shadow-lg transition-all duration-200 flex items-center gap-2" do %>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
          <span>Back to Restaurants</span>
        <% end %>
      <% end %>
    </div>
  </div>

  <%= render "form", restaurant: @restaurant %>
</div>
